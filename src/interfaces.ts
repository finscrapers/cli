export interface IBotData {
  data: string;
}

export interface IMeta {
  index: string;
  type: string;
}

export interface IOptions {
  headless: boolean;
  slowMo: number;
}

export interface ISymbol {
  Symbol?: string;
}
